[pytest]
# Avoid collecting generated/nested worktrees that mirror test module names
# and trigger import-mismatch errors during full-suite discovery.
norecursedirs =
    data/worktrees
    .codexbot_tmp
    .codexbot_preview
    .git
    .venv
    venv
    node_modules
